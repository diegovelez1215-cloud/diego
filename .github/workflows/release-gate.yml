name: release-gate

on:
  push:
    branches: ['**']
  pull_request:

jobs:
  logic-tests:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    env:
      PLAYWRIGHT_SKIP_BROWSER_DOWNLOAD: '1'
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - run: npm ci
      - name: Logic + truth suite
        run: npm run test:logic
      - name: Whitespace hygiene
        run: git diff --check
